import {TrainingProgressUpdate} from "@shared/user_progress";

export const loadUserProgress = async (progressId: {userId: string, courseId: string}) => {
    return null;
};

export const saveUserProgress = async (update: TrainingProgressUpdate) => {
    return null;
};
